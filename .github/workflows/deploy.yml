name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Sync images
        run: |
          rm -rf docs/img
          mkdir -p docs/img
          cp -r src/img/* docs/img/